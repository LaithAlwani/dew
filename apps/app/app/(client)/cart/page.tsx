import { PagePlaceholder } from "@dew/ui";

export default function Page() {
  return (
    <PagePlaceholder
      badge="Cart"
      title="Shopping list"
      subtitle="A guided beauty list with context, not random e-commerce."
    />
  );
}
